#One day I will figure out how this all works
FROM node:argon

#Create the application directory
RUN mkdir -p /usr/src
COPY . /usr/src
WORKDIR /usr/src

#Installs
RUN npm install
RUN npm install -g nodemon

#Expose a port
EXPOSE 80

#Run
CMD ["nodemon", "index.js"]
